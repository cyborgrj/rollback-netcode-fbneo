<Window x:Class="RbfLauncher.Views.ConnectDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Conectar ao servidor" Width="460" SizeToContent="Height"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="{StaticResource Bg}">
  <Grid Margin="24">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" /><RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" /><RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" /><RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*" /><ColumnDefinition Width="110" />
    </Grid.ColumnDefinitions>

    <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="Nome do jogador" Foreground="{StaticResource TextDim}" />
    <TextBox   Grid.Row="1" Grid.ColumnSpan="2" x:Name="UserBox" Margin="0,4,0,14" Padding="6,5" />

    <TextBlock Grid.Row="2" Text="Servidor (IP ou host)" Foreground="{StaticResource TextDim}" />
    <TextBlock Grid.Row="2" Grid.Column="1" Text="Porta" Foreground="{StaticResource TextDim}" />
    <TextBox   Grid.Row="3" Grid.Column="0" x:Name="HostBox" Margin="0,4,8,4" Padding="6,5" />
    <TextBox   Grid.Row="3" Grid.Column="1" x:Name="PortBox" Margin="0,4,0,4" Padding="6,5" />

    <TextBlock Grid.Row="4" Grid.ColumnSpan="2" x:Name="ErrorText" Foreground="{StaticResource Warn}"
               FontSize="12" Margin="0,8,0,0" TextWrapping="Wrap" />

    <StackPanel Grid.Row="6" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
      <Button Content="Cancelar" Style="{StaticResource Btn}" MinWidth="90" Click="Cancel_Click" />
      <Button Content="Conectar" Style="{StaticResource BtnPrimary}" MinWidth="90" Margin="10,0,0,0" Click="Ok_Click" />
    </StackPanel>
  </Grid>
</Window>
