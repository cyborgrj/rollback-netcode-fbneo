# Upstream FBNeo checkout — its own git repo, pinned via FBNEO_PIN.txt, not vendored here
/fbneo/

# Build output
/build/
/out/
*.o
*.obj
*.a
*.lib
*.dll
*.exe
*.pdb
*.ilk

# .NET (launcher / server)
[Bb]in/
[Oo]bj/
out/
publish/
publish-server/
*.user
rbf-launcher.json

# rom-database files are the user's own, never committed (any json_roms/ folder)
**/json_roms/*.json

# ROMs are never committed — copyrighted, and the project must not carry them
*.zip
/rollback_netcode/roms/
**/roms/arcade/

# Editor / OS
.vscode/
.idea/
*.swp
*~
.DS_Store
Thumbs.db

# lixo que o build do WPF deixa para tras
*_wpftmp.csproj
